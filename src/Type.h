#pragma once

#include <string>

enum Type {
    VOID = 0,
    INT32_T = 1,
    INT64_T = 2,
    CHAR = 3,
    ERROR = -1
};

enum TypeVariable {
    NAME = 0,
    ENTIER = 1,
    CARACTERE = 2,
};

enum Operateur {
    EQUAL = 0,
    PLUS = 1
};
